# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example

# BD SQLite local descartable que usan las pruebas (tests/setup.ts) — nunca es la BD real.
/.tmp/
update-api/.tmp/

# Secrets locales del Worker (update-api/) — equivalente a .env pero con la
# convención de nombre que usa wrangler.
update-api/.dev.vars
update-api/.dev.vars.*
!update-api/.dev.vars.example
update-api/.wrangler/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
