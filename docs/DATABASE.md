# Base de datos

Turso (libSQL/SQLite hosteado). Sin migration runner: el esquema se creó completo con un script one-off. Toda la app accede vía `src/db.ts` — ningún otro archivo importa `@libsql/client`.

Patrón dominante: **replace-and-reinsert** en cada guardado para tablas hijas de lista libre (specs, checks, extras, plastic items, user_permissions) — se borra todo lo del padre y se reinserta. Excepciones: `plastic_products` (master data, nunca se borra-reinserta, solo se actualiza), y las tablas insert-only (`requisiciones`, `folios`, `product_print_item_orders/purchases`, `app_logs`) que son historial/log, nunca se reemplazan. `remisiones`/`remision_renglones` fueron insert-only hasta que se agregó edición/borrado explícitos (`updateRemisionConRenglones`/`deleteRemision`, gateados con permiso — ver [PERMISSIONS.md](PERMISSIONS.md)): `updateRemisionConRenglones` sí usa replace-and-reinsert para `remision_renglones` (mismo patrón que specs), pero solo como acción deliberada del usuario, nunca como efecto secundario de leer/mostrar la remisión — el snapshot de precio/nombre de cada renglón sigue sin recalcularse solo automáticamente contra `precios`/`products`. `deleteRemision` sigue el mismo precedente que `deletePrintItemOrder`/`deletePrintItemPurchase`: borrado real, no un flag.

`createRemisionConFolio`/`createRequisicionConFolio` usan `client.transaction()` (transacción interactiva de libSQL, no `client.batch()`) para generar el folio **y** escribir el documento en un solo paso atómico — evita que un folio quede "huérfano" (consumido pero sin documento) si el insert falla justo después. `createProduct`/`updateProduct` usan el mismo mecanismo para que specs/descriptions nunca queden a medias respecto al producto — relevante sobre todo en Captura masiva, que corre fila tras fila sin supervisión. `createFolio` standalone sigue existiendo y sigue siendo un paso separado del insert del documento en Compra/Producción/Requisición (`CompraForm`/`OrderModal`/`ProduccionForm`): ahí el folio debe imprimirse en el PDF *antes* de un diálogo de guardado que el usuario puede cancelar o tardar en resolver, así que no conviene mantener una transacción de escritura abierta esperando esa interacción — un folio quemado sin documento en esos tres flujos es una compra/orden abandonada por el usuario, no una falla de integridad.

## Tablas activas

| Tabla | Columnas | Notas |
|---|---|---|
| `products` | `codigo` (unique), `nombre`, `categoria`, `material`, `descripcion`, `imagen`/`imagen_mime` (BLOB), `imagen_codigo_barras`/`imagen_codigo_barras_mime` (BLOB), `presentacion_original`, `creado_en`, `actualizado_en` | `presentacion_original` guarda el texto crudo de la celda de importación Excel para auditoría; `actualizado_en` se refresca en cada UPDATE. `descripcion` es específicamente la descripción de **Catálogo** — las demás viven en `product_descriptions`. `imagen_codigo_barras`/`imagen_codigo_barras_mime` (capturado en `ProductForm`, justo debajo de la imagen principal) reemplaza al código de barras que antes vivía en `plastic_products` — ver "Tablas y columnas muertas" abajo |
| `product_specs` | `product_id`, `etiqueta`, `valor`, `orden`, `permite_requisicion` | Schemaless a propósito (distintos productos necesitan distintos specs). `permite_requisicion` controla si esa fila muestra el botón "Requisición" en `ProductDetail` |
| `product_descriptions` | `product_id`, `etiqueta`, `texto`, `orden` | Descripciones adicionales de la ficha (Página web, Licitación, y cualquier tipo que el usuario agregue) — Catálogo NO está aquí, vive en `products.descripcion`. Schemaless igual que `product_specs`; `src/descriptions.ts` combina ambas fuentes para la UI (`buildDescriptionSlots`/`ensureFixedDescriptions`) |
| `plastic_products` | `nombre`, `sku`, `color`, `origen`, `descripcion`, `armado`, `dimension`, `peso`, `tipo_empaque`, `maquila`, `coste`, `imagen`/`imagen_mime`, `creado_en` | Catálogo maestro "Piezas" (reutilizable entre fichas). `createPlasticProduct`/`updatePlasticProduct` nunca forman parte del ciclo replace-and-reinsert. La columna `armado` se expone como `material` en `PlasticProduct`/`PlasticProductInput` — no se renombró la columna (BD compartida en vivo, sin migration runner) — ver mapeo en `rowToPlasticProduct`/`plasticProductToData` y "Mapeo de columnas heredado" abajo. `sku` no es `NOT NULL` ni único: pueden existir piezas sin SKU (`PiezasGeneralSection`). `maquila`/`coste` son texto libre, mismo tratamiento que `dimension`/`peso`/`tipo_empaque` |
| `product_plastic_items` | `product_id`, `plastic_product_id`, `orden` | Join ficha↔pieza. Quitar de una ficha solo borra esta fila, no la maestra |
| `product_print_items` | `nombre`, columnas de producción (ver mapeo abajo), `numero_placas`, `placas_existentes` (`""\|"si"\|"no"`), `acabados`, `notas`, `orden` | Todo `TEXT`, sin parseo de unidades |
| `product_print_item_checks` | `print_item_id`, `nombre`, `marcado` (0/1), `orden` | Exactamente los 5 nombres fijos de `PROCESOS_IMPRENTA` en `types.ts`; `getPrintItems` siempre normaliza a esa lista/orden — la UI no permite agregar/renombrar checkboxes |
| `product_print_item_extras` | `print_item_id`, `etiqueta`, `valor`, `orden` | Segmentos libres label/value ("+ Agregar otro segmento") |
| `product_print_item_images` | `print_item_id`, `imagen`/`imagen_mime` (BLOB), `orden` | "Imágenes de armado", tope = `numero_pliegos` |
| `product_print_item_orders` | `print_item_id`, `merma`, `cantidad_arte`, `numero_tiros`, `formacion_usada`, `numero_pliegos_usado`, `total_pliegos`, `usuario`, `folio`, `creado_en` | Una fila por orden de producción generada. Ver [WORKFLOWS.md](WORKFLOWS.md) |
| `product_print_item_purchases` | `print_item_order_id` (FK), `papel`, `pliego`, `maquina`, `cortes`, `cantidad`, `total_tamanos`, `usuario`, `folio`, `creado_en` | Una fila por orden de compra, siempre ligada a una orden de producción |
| `users` | `username` (unique), `password_hash` (bcrypt), `activo`, `rol` (`usuario`\|`admin`), `creado_en`, `failed_attempts`, `locked_until`, `session_token`, `session_expires_at`, `backup_local_diario` | Login con bloqueo y sesión por token con vencimiento deslizante — ver [PERMISSIONS.md](PERMISSIONS.md). `backup_local_diario` (0/1, otorgado en `UsersPanel`, no es un permiso de `PERMISOS`) dispara un backup local automático al entrar a la app — ver [DISASTER_RECOVERY.md](DISASTER_RECOVERY.md#backup-local-diario-por-usuario) |
| `user_permissions` | `user_id`, `permiso` | Texto libre validado contra `PERMISOS` en `types.ts` |
| `user_sessions` | `user_id` (PK) → `last_seen` | Una fila por **cuenta**, no por dispositivo. Heartbeat — ver [ARCHITECTURE.md](ARCHITECTURE.md) |
| `app_logs` | `nivel`, `mensaje`, `usuario`, `creado_en` | `clearLogs()` permite vaciarla desde `LogsPanel` ("Limpiar"). Sin rotación automática |
| `folios` | `seccion` (`requisicion`\|`compra`\|`produccion`\|`remision`), `consecutivo`, `folio`, `sku`, `creado_en` | `consecutivo` por sección, nunca reinicia. Solo `createFolio` — no hay listado histórico |
| `requisiciones` | `product_id`, `fecha`, `numero_dia` (reinicia diario), `usuario`, `etiqueta`, `descripcion`, `cantidad`, `estado`, `mensaje`, `folio`, `creado_en` | Solo `createRequisicion` existe — nada lee/lista/actualiza `estado` después del insert (ver [MODULES.md](MODULES.md)) |
| `backup_history` | `tipo` (`BACKUP_AUTOMATICO`\|`BACKUP_MANUAL`\|`BACKUP_PRE_IMPORTACION`\|`BACKUP_PRE_RESTAURACION`\|`RESTAURACION`\|`RESTAURACION_ARCHIVO_SUBIDO`\|`CONFIGURACION_CAMBIADA`), `origen`, `usuario`, `archivo`, `ubicacion`, `tamano_bytes`, `checksum_sha256`, `estado` (`EN_PROCESO`\|`EXITOSO`\|`FALLIDO`), `detalle`, `creado_en` | Insert-only, como `requisiciones`/`app_logs` — nunca se reemplaza. `ubicacion` guarda la ruta local absoluta O la URL del asset de GitHub Release, según dónde vive ese backup — no solo la palabra "local"/"github". Ver [DISASTER_RECOVERY.md](DISASTER_RECOVERY.md) |
| `backup_settings` | Fila única (`id=1`): `automatico_activado`, `frecuencia`, `hora_ejecucion`, `intervalo_horas`, `dia_semana`, `retencion_diaria_dias`, `retencion_semanal_dias`, `retencion_mensual_dias`, `ultimo_automatico_en`, `actualizado_en`, `actualizado_por` | Leída tanto por Clio (panel de Programación) como por el workflow de GitHub Actions del repo separado `clio-backups` — es la fuente de verdad de "cuándo toca" un backup automático, no un cron fijo en YAML |
| `precios` | `sku` (unique), `sku_principal` (SKU sin las letras finales — ver `computeSkuPrincipal` en `src/precios.ts`), `nombre`, `precio`, `actualizado_en`, `actualizado_por`, `creado_en`, `tipo` (`interno`\|`externo`\|`NULL`) | Un SKU con letra (`8059C`) es su propia fila, agrupada bajo `sku_principal="8059"` — no requiere que exista un producto con código `8059`. `tipo` NULL = productos guardados antes de esta clasificación, tratados como `interno` en todas partes (nunca como `externo`) — ver exportación en `excelExport.ts` |
| `precios_historial` | `sku`, `precio_anterior`, `precio_nuevo`, `usuario`, `creado_en` | Insert-only, se escribe en cada `upsertPrecio`. Independiente del snapshot de precio que guarda cada renglón de remisión — no confundir los dos |
| `remisiones` | `folio`, `fecha`, `tipo` (`interna`\|`externa`), `pedido_bodegas`, `cancelada`, `subtotal`, `descuento_pct`, `descuento`, `iva`, `total`, `precio_texto`, `usuario`, `creado_en` | Cabecera del documento. Solo existe el flujo `interna` — `tipo` está preparado para `externa` pero nada la escribe todavía. `pedido_bodegas` es un campo de texto libre ("Bodega" en el formulario), precargado con `"JALISCO"` (constante `PEDIDO_BODEGAS_INTERNA` en `RemisionForm.tsx`) pero editable tanto al crear como al editar. `cancelada` es histórico: filas antiguas pueden tenerlo en 1, pero nada en la UI lo vuelve a escribir desde que "Cancelar" se reemplazó por "Borrar" (`deleteRemision`, borra la fila) — folio, fecha y `creado_en` nunca cambian una vez creada la fila, ni siquiera al editar (`updateRemisionConRenglones` los conserva; sí actualiza `pedido_bodegas` y los totales) |
| `remision_renglones` | `remision_id` (FK), `numero_renglon`, `sku`, `producto_nombre`, `cantidad`, `precio_unitario`, `importe` | `sku`/`producto_nombre`/`precio_unitario` son un snapshot congelado al crear la remisión — nunca se vuelven a leer de `products`/`precios` después del insert, ni siquiera si el precio del producto cambia después |
| `pending_product_images` | `codigo` (unique), `imagen`/`imagen_mime` (BLOB), `archivo_original`, `creado_por`, `creado_en` | Imágenes de la importación masiva cuyo código no correspondía a ninguna ficha en ese momento — se aplican solas la próxima vez que `createProduct` inserte ese `codigo` (`applyPendingProductImage`), y se borran de aquí al aplicarse. Ver [WORKFLOWS.md](WORKFLOWS.md#importación-de-imágenes) |

## Mapeo de columnas heredado (`plastic_products`)

| Campo en `types.ts` | Columna real |
|---|---|
| `material` | `armado` |

Antes "Armado" en la UI de Piezas; se renombró a "Material" (visual y en `types.ts`/componentes) sin tocar la columna en Turso para no arriesgar datos de piezas ya registradas.

## Mapeo de columnas heredado (`product_print_items`)

El tipo `PrintItem` no coincide 1:1 con las columnas (compatibilidad con filas antiguas):

| Campo en `types.ts` | Columna real |
|---|---|
| `tamano_extendido` | `extendido` |
| `tamano_final` | `tamano` |
| `cortes_tamano` | `corte_cm` |

El resto (`tintas`, `tipo_papel`, `gramos_puntos`, `pliego`, `maquina`, `formacion`, `numero_pliegos`) mapea directo.

## Tablas y columnas muertas — no resucitar sin confirmar con el usuario

- `product_plastic_pieces` + `getPlasticPieces`/`savePlasticPieces` en `db.ts`: reemplazadas por `plastic_products`/`product_plastic_items`.
- `section_passwords`: del viejo sistema de contraseña por sección (pre-usuarios).
- Columnas sin usar en `product_print_items`: `cantidad`, `numero_cortes`, `total`, `merma`, `buenos`, `pliegos_imprimir`, `cada_pliego`, `total_artes`, `existencias`, `total_general`.
- `plastic_products.imagen_codigo_barras`/`imagen_codigo_barras_mime`: el código de barras se movió de Piezas a la ficha técnica (`products.imagen_codigo_barras`/`imagen_codigo_barras_mime`, ver fila de `products` arriba) — la app ya no lee ni escribe estas dos columnas de `plastic_products`, el dato que tuvieran queda huérfano.
- `@tauri-apps/plugin-sql` sigue en `package.json` pero no se usa en ningún lado (la DB se accede por HTTPS/WebSocket vía `@libsql/client`, no por plugin de Tauri).

No se eliminan por ser cambios de esquema/dependencias en una BD compartida en vivo con datos reales, no por descuido.

## Funciones de `db.ts` por dominio

- **Products/specs**: `searchProducts`, `getProduct`, `getProductSpecs`, `getProductDescriptions`, `createProduct`, `updateProduct` (ambas reciben `descriptions` además de `specs`; ambas atómicas vía `client.transaction()`), `deleteProduct` (cascada a specs/descriptions/plastic items/print items/orders/purchases/images, atómica vía `client.transaction()`), `findProductByCodigo`, `findProductsByNombre`, `setPresentacionOriginal`, `codigoEnUso`. `createProduct`/`updateProduct`/`deleteProduct` reciben un `Actor` como primer argumento pero solo exigen sesión vigente (`assertActorSession`, sin permiso específico — catálogo base es intencionalmente abierto, ver [PERMISSIONS.md](PERMISSIONS.md)). `createProduct` también aplica sola, dentro de la misma transacción, cualquier imagen pendiente para ese `codigo` (`applyPendingProductImage`/`upsertPendingProductImage`, tabla `pending_product_images` — ver [WORKFLOWS.md](WORKFLOWS.md#importación-de-imágenes)).
- **Imágenes**: `getImageSrc`, `pickImage`, `updateProductImage`, `pickExcelFile`, `pickImageFolder`, `listImageFolderFiles`, `readImageFileBlob`, `MIME_BY_EXT`. `pickImage`/`readImageFileBlob` validan la firma real de los bytes (magic numbers) contra png/jpg/webp/gif y un tamaño máximo (`MAX_IMAGE_FILE_BYTES`) — no confían en la extensión del archivo. `pickExcelFile` exige extensión `.xlsx`, firma ZIP real y `MAX_EXCEL_IMPORT_FILE_BYTES`.
- **Usuarios/auth**: `verifyLogin` (retorna `ok|invalid|locked`), `validateSession`, `listUsers`, `usernameEnUso`, `createUser`/`updateUser` (reciben un `Actor` `{id, token}` como primer argumento — ver [PERMISSIONS.md](PERMISSIONS.md)).
- **Sesiones**: `heartbeat`, `clearSession`, `getConnectedUsers`.
- **Logs**: `logEvent`, `getRecentLogs`, `clearLogs`.
- **Piezas (catálogo)**: `searchPlasticProducts`, `getPlasticProduct` (una sola pieza por `id`, usada por `PiezaDetalleScreen`), `createPlasticProduct`, `updatePlasticProduct`, `deletePlasticProduct` (borra la pieza y en cascada sus filas en `product_plastic_items` — atómico vía `client.transaction()`, mismo patrón que `deleteProduct`), `getProductsUsingPlasticProduct` (join inverso: fichas que tienen esta pieza vinculada, para avisar antes de borrar/editar), `getPlasticItems`, `savePlasticItems`. `createPlasticProduct`/`updatePlasticProduct`/`deletePlasticProduct`/`savePlasticItems` reciben un `Actor` (permiso `plasticos`, ver [PERMISSIONS.md](PERMISSIONS.md)).
- **Imprenta (ficha)**: `getPrintItems`, `savePrintItems`.
- **Imprenta (órdenes)**: `createPrintItemOrder`, `getPrintItemOrders`, `createPrintItemPurchase`, `getPrintItemPurchases`, `deletePrintItemOrder` (atómica vía `client.transaction()`), `deletePrintItemPurchase`. `savePrintItems`/`createPrintItemOrder`/`createPrintItemPurchase`/`deletePrintItemOrder`/`deletePrintItemPurchase` reciben un `Actor` (permiso `imprenta`, ver [PERMISSIONS.md](PERMISSIONS.md)).
- **Folios**: `createFolio` (standalone, ver nota arriba sobre Compra/Producción/Requisición).
- **Requisiciones**: `createRequisicion` (recibe el folio ya creado), `createRequisicionConFolio` (genera folio + inserta en una sola transacción — preferir esta variante cuando el llamador no necesita el folio antes de un paso externo como un diálogo de guardado).
- **Precios**: `upsertPrecio` (INSERT...ON CONFLICT, también escribe `precios_historial`; `tipo` es opcional en el input — si se omite al actualizar un SKU existente, conserva el que ya tenía vía `COALESCE(?, precios.tipo)`, nunca lo borra por una simple edición de precio; recibe un `Actor` y exige `precios_modificar` **o** `remisiones_crear` — se llama también desde "Guardar producto" en `RemisionForm`), `updatePrecio` (edita por `id`, no por `sku` — es la única forma de renombrar el SKU de una fila existente; valida que el nuevo SKU no choque con otra fila antes de escribir, ya que `sku` es `UNIQUE`; recibe un `Actor`, exige `precios_modificar`), `getPrecio`, `getPreciosBySkuPrincipal`, `getPreciosList`, `searchPrecios` (SKU normal o con letra / nombre — usada por el buscador de `RemisionForm`, no por `searchProducts`).
- **Remisiones**: `createRemisionConFolio` (folio + header + renglones en un solo `client.transaction()` — ver nota arriba), `listRemisiones`, `getRemisionRenglones`, `updateRemisionConRenglones` (edita header/renglones de una remisión existente conservando folio/fecha/`creado_en`/`usuario` — nunca genera folio nuevo ni una segunda fila), `deleteRemision` (borrado real vía transacción, no un flag), `listRemisionRenglonesParaHistorial` (join usado por la exportación "Historial de remisiones"). `createRemisionConFolio`/`updateRemisionConRenglones` reciben un `Actor` (permiso `remisiones_crear`); `deleteRemision` recibe un `Actor` (permiso `remisiones_cancelar`).
- **Backups**: `createBackupSql`/`verifyRestoreCounts` (dump y verificación crudos), `executeRestoreSql`/`deleteBackupRecord`/`updateBackupSettings` (reciben un `Actor` como primer argumento, ver [PERMISSIONS.md](PERMISSIONS.md)), `runBackupNow` (orquesta dump+verificar+guardar+registrar — un solo punto de entrada usado por el botón manual y por el hook de pre-importación; a propósito **no** exige `Actor`, ver PERMISSIONS.md), `createBackupRecord`/`updateBackupRecord`/`listBackupHistory`/`getLatestBackup` (tabla `backup_history`), `getBackupSettings` (tabla `backup_settings`), `getBackupsDir`/`saveLocalBackupFile`/`readLocalBackupFile`/`localBackupFileExists`/`deleteLocalBackupFile`/`saveBackupFileAs`/`pickBackupFile` (I/O local bajo el directorio de datos de la app). `executeRestoreSql` valida cada statement contra `validateRestoreStatements()` (subconjunto seguro: `DROP TABLE IF EXISTS`/`CREATE TABLE`/`INSERT INTO` sobre una tabla que ya existe en `sqlite_master` — ver [DISASTER_RECOVERY.md](DISASTER_RECOVERY.md#restauración)) antes de ejecutar `client.migrate()`. La lógica pura de armado/parseo/validación de dumps (escapado SQL, split de statements, checksum, gzip, subconjunto seguro de restauración) vive en `src/backup.ts`, sin tocar la BD, para poder reusarse igual en el script Node del repo `clio-backups`. Ver [DISASTER_RECOVERY.md](DISASTER_RECOVERY.md).

Agregar queries nuevas aquí, nunca inline en un componente.
