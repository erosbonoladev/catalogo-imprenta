# Módulos — estado real

Estados: **IMPLEMENTADO** · **EN DESARROLLO** (parcialmente conectado) · **PLANEADO** (decidido, sin código) · **NO IMPLEMENTADO** (no existe, aunque el tipo/tabla exista).

No asumir que algo existe por aparecer en una conversación previa o en un doc viejo — este archivo se corrige contra el código real cuando cambie.

| Módulo | Estado | Gate | Detalle |
|---|---|---|---|
| Fichas técnicas (catálogo base) | IMPLEMENTADO | Ninguno | CRUD completo, `SearchScreen`→`ProductDetail`→`ProductForm` |
| Piezas (catálogo, antes "Plásticos") | IMPLEMENTADO | `plasticos` | Reutilizable entre fichas vía `PlasticProductPicker`. Campo "Armado" renombrado a "Material" en la UI (columna real sigue siendo `armado`, ver [DATABASE.md](DATABASE.md)) |
| Piezas General (catálogo maestro completo, fuera de una ficha) | IMPLEMENTADO | `plasticos` (mismo permiso que Piezas) | `PiezasGeneralSection`, entrada propia en `Sidebar` debajo del toggle de tema. Tabla tipo Excel sobre `searchPlasticProducts("")` (todas las piezas, con o sin ficha asociada), con acciones por fila: Ver especificaciones (abre `PiezaDetalleScreen`, pantalla propia con todos los campos, imágenes a mayor tamaño y lista de fichas donde se usa vía `getProductsUsingPlasticProduct`), Editar y Borrar (`deletePlasticProduct`, con confirmación inline). Alta/edición reutilizan el mismo `PiezaFormModal`/`PlasticProductFields` que "Agregar producto nuevo" dentro de una ficha. SKU no obligatorio |
| Imprenta — ficha técnica de producción | IMPLEMENTADO | `imprenta` | Specs fijos + checks + extras + "imágenes de armado" |
| Imprenta — historial de órdenes de producción/compra (ver, editar conservando folio + regenerar PDF, borrar) | IMPLEMENTADO | `imprenta` | Ver [WORKFLOWS.md](WORKFLOWS.md#producción--compra) |
| Maderas (catálogo de productos de madera por ficha) | IMPLEMENTADO | `maderas` | `MaderasSection`, botón "Maderas" en `ProductDetail` junto a Piezas/Imprenta. Mismo patrón que Piezas: catálogo maestro `wood_products` + puente `product_wood_items`, editable vía botón "Editar" (dirty/Toast). Sin pantalla de catálogo general tipo CRUD propia (no hay "Maderas General" — a diferencia de Piezas, no se pidió; sí existe "Cortes de Madera" como calculadora, ver abajo, que lee el mismo catálogo pero no lo edita). Campo "Tamaño" se muestra como "Pieza" y Largo/Ancho se agrupan visualmente como "Armado" (columnas reales sin cambios, ver [DATABASE.md](DATABASE.md)). Incluye una subsección de solo lectura con las piezas de Piezas cuyo nombre/material sugiere madera/MDF, sin duplicarlas — ver [PERMISSIONS.md](PERMISSIONS.md) y [DATABASE.md](DATABASE.md) |
| Cortes de Madera (calculadora de aprovechamiento de placas MDF) | IMPLEMENTADO | `maderas` (mismo permiso que Maderas) | `CortesMaderaSection`, entrada propia en `Sidebar` debajo de "Piezas General". Placa fija 122×244cm; busca piezas de `wood_products` (`searchWoodProducts`, sin escritura) o admite captura manual de Ancho/Largo sin persistir en BD; modo "Pieza" (parsea una medida "NNxNN", Ancho×Largo, del campo Pieza) o "Armado" (usa Ancho×Largo directos); Gramaje fijo 2.5/5.5; separación real = margen de 3mm de la cortadora (checkbox, activado por defecto, el usuario decide si incluirlo) + margen adicional del usuario (línea de corte con grosor mínimo fijo en pantalla para que siempre se vea, aunque el margen real sea sub-pixel a esa escala). Algoritmo de acomodo 100% cliente en `src/corteMaderaLayout.ts` (`computeSheetLayout(piece, marginMm, incluirMargenCortadora?)`, búsqueda recursiva de combinaciones por regiones en ambas orientaciones — prueba todos los tamaños de bloque posibles, no solo el máximo, porque usar menos columnas/filas a veces deja una franja sobrante que rinde más en la otra orientación, determinista — nunca reserva margen en los bordes de la placa, solo entre piezas), representación visual en SVG (`PlacaVisual.tsx`) |
| Importación masiva de maderas (Excel) | IMPLEMENTADO | `maderas` (no `isAdmin`, mismo criterio que Piezas) | `MaderaImportPanel.tsx`/`maderaImport.ts`, pestaña "Maderas" en Captura masiva. El SKU de cada fila identifica al producto/juego (`computeSkuPrincipal`, mismo criterio que Precios/Remisiones) — no a la pieza, que se repite en varias filas cuando un juego tiene varias piezas de madera. Duplicado dentro de un juego: nombre+tamaño. Ver [WORKFLOWS.md](WORKFLOWS.md#importación-de-maderas-excel) |
| Eliminar última importación masiva de maderas | IMPLEMENTADO | `maderas` | Mismo patrón que Piezas — solo deshace lo **creado** por la corrida más reciente, vía `madera_import_batches`/`undoLastMaderaImportBatch` |
| Requisiciones — crear y enviar por WhatsApp | IMPLEMENTADO | `requisiciones` | Ver [WORKFLOWS.md](WORKFLOWS.md#requisiciones) |
| Requisiciones — seguimiento de estado tras creación | **NO IMPLEMENTADO** | — | `ESTADOS_REQUISICION` existe en `types.ts` pero ninguna función de `db.ts` lee/lista/actualiza `estado`; no hay pantalla de seguimiento. Toda requisición queda en `pendiente` para siempre en la BD |
| Folios (numeración de documentos) | IMPLEMENTADO (solo escritura) | — | Sin listado/consulta histórica. Escrito por `createFolio` (Compra/Producción/Requisición) o dentro de `createRemisionConFolio`/`createRequisicionConFolio` (ver [DATABASE.md](DATABASE.md)) |
| Importación masiva de fichas (Excel) | IMPLEMENTADO | `isAdmin` (no es permiso otorgable) | Usa `xlsx`; ver [WORKFLOWS.md](WORKFLOWS.md#importación-de-fichas-excel) |
| Importación de corrección de fichas + Precios Venta (Excel) | IMPLEMENTADO | `isAdmin` | `CorreccionImportPanel.tsx`/`correccionImport.ts`, pestaña "Datos y Precios Venta" en Captura masiva. Corrige Nombre/Categoría/Tipo de producto/Código de barras de fichas existentes (por SKU=`codigo` exacto) y carga Gobierno/Representante/Mayoreo/Medio mayoreo/Público sugerido en `precios_venta`; si el SKU no existe, da de alta una ficha nueva (material/descripción/specs/imagen quedan vacíos). "Por definir" en una celda de precio es válido: guarda `NULL` explícito en esa categoría. Ver [WORKFLOWS.md](WORKFLOWS.md#importación-de-corrección-de-fichas--precios-venta-excel) |
| Importación masiva de imágenes | IMPLEMENTADO | `isAdmin` | Empareja por nombre completo o por SKU recortado al inicio del nombre = `codigo` (ver [WORKFLOWS.md](WORKFLOWS.md#importación-de-imágenes)). Soporta png/jpg/webp/gif. Código sin ficha todavía → imagen guardada en `pending_product_images`, se aplica sola al crear ese producto |
| Importación masiva de imágenes de código de barras | IMPLEMENTADO | `isAdmin` | `BarcodeImageImportPanel.tsx`/`barcodeImageImport.ts`, pestaña "Código de barras" en Captura masiva. Carpeta raíz con una subcarpeta por código (nombrada con el número); dentro de cada una, único archivo válido = el que se llama exactamente igual al número (solo dígitos, sin "-" adelante), png/jpg/webp/gif/**svg**. Empareja por `codigo_barras_texto` exacto, no por SKU. Código de barras sin ficha todavía → imagen guardada en `pending_product_barcode_images`, se aplica sola al crear un producto con ese `codigo_barras_texto` (mismo mecanismo que `pending_product_images`, ver abajo). Archivo ambiguo o carpeta sin archivo válido → se reporta y se omite (esos sí, sin mecanismo de pendiente). `SearchScreen` busca por `codigo_barras_texto` tanto en el filtro "Todo" como en un filtro dedicado "Código de barras" (exacto, sin mezclar con nombre/SKU/material). Ver [WORKFLOWS.md](WORKFLOWS.md#importación-de-imágenes-de-código-de-barras) |
| Importación masiva de piezas (Excel, dos formatos: agrupado por bloques del proveedor, o plano "Desglose" reimportando un export de SKU Master depurado) | IMPLEMENTADO | `plasticos` (no `isAdmin`, a diferencia de fichas/imágenes/precios) | `PiezasImportPanel.tsx`/`piezasImport.ts`, pestaña "Piezas" en Captura masiva. Detecta el formato por nombre de hoja: una hoja llamada "Desglose" manda (plana, una fila por pieza, sin imágenes, emparejamiento con respaldo por posición); si no existe, formato clásico del proveedor (fila con SKU sin guion = juego, sus piezas heredan ese juego hasta la siguiente, imágenes por link de Google Drive/Photos descargadas vía `@tauri-apps/plugin-http`). Revisión: conteos claros de nuevas/repetidas/sin relación, filtro de solo vista por estado, botón "Importar solo lo nuevo", y detección de posible duplicado contra TODO el catálogo (no solo el juego actual) por SKU o nombre para las filas "nueva" — el usuario decide fila por fila si vincula la pieza existente o crea una nueva, nunca se fusiona sola. Ver [WORKFLOWS.md](WORKFLOWS.md#importación-de-piezas-excel-dos-formatos) |
| Eliminar última importación masiva de piezas | IMPLEMENTADO | `plasticos` | Solo deshace piezas **creadas** por la corrida más reciente (no las actualizadas), vía `piezas_import_batches`/`undoLastPiezaImportBatch` — ver [WORKFLOWS.md](WORKFLOWS.md#importación-de-piezas-excel-dos-formatos) |
| Exportación a PDF (orden/compra/requisición) | IMPLEMENTADO | — | `jspdf`, ver [WORKFLOWS.md](WORKFLOWS.md#pdf) |
| Tema claro/oscuro | IMPLEMENTADO | — | Toggle en Sidebar, persiste en `localStorage` |
| Barra lateral (Sidebar) | IMPLEMENTADO | — | Es chrome persistente (tema, atajos a Remisiones/SKU Master/Piezas General/Cortes de Madera, logout, config, updater) — **no** es un menú de navegación entre pantallas. Esas cuatro son filas ícono+texto debajo del toggle de tema (con divisor compartido), en ese orden — Cortes de Madera va debajo de Piezas General, gateada por `maderas`. Configuraciones sigue siendo el ícono cuadrado de abajo |
| Usuarios/Configuraciones/Registro/Conectados | IMPLEMENTADO | `configuraciones` | Ver [PERMISSIONS.md](PERMISSIONS.md) |
| Auto-actualización | IMPLEMENTADO | — | `@tauri-apps/plugin-updater`+`plugin-process`, botón en Sidebar. Desde este cambio, el endpoint es el Update API propio (`update-api/`), no GitHub directo — ver [DISTRIBUTION.md](DISTRIBUTION.md) |
| Activación de instalaciones / distribución privada | IMPLEMENTADO (pendiente desplegar Worker + migrar repo a privado — ver checklist manual en [DISTRIBUTION.md](DISTRIBUTION.md)) | Gate de máquina, previo al login (`ActivationProvider`/`ActivationScreen`) | Credencial de activación de 256 bits generada por un admin, consumida una vez, credencial local en Keychain/Credential Manager vía `keyring`. Ventana de gracia offline de 7 días |
| Administración de instalaciones (crear/revocar/reactivar credenciales) | IMPLEMENTADO | `instalaciones_ver`/`instalaciones_crear_credenciales`/`instalaciones_revocar`/`instalaciones_reactivar` (otorgables individualmente, mismo criterio que `backups_*`) | `InstalacionesPanel`, tab "Instalaciones" dentro de Configuraciones — ver [PERMISSIONS.md](PERMISSIONS.md) y [DISTRIBUTION.md](DISTRIBUTION.md) |
| Modo offline | NO IMPLEMENTADO | — | Constraint aceptada, ver [ARCHITECTURE.md](ARCHITECTURE.md) |
| Optimización/resize de imágenes | NO IMPLEMENTADO | — | Se guardan tal cual se importan |
| Backups — manual, pre-importación, restauración (desde historial y archivo subido) | IMPLEMENTADO | `backups_*` (otorgables individualmente) | `BackupsPanel.tsx` dentro de Configuraciones. Ver [DISASTER_RECOVERY.md](DISASTER_RECOVERY.md) |
| Backups — automático programado | IMPLEMENTADO, pero **desactivado por defecto** (`backup_settings.automatico_activado = 0`) | `backups_configurar` para prenderlo | Corre en un repo GitHub separado (`clio-backups`), no dentro de Clio ni de este repo — ver [DISASTER_RECOVERY.md](DISASTER_RECOVERY.md) |
| Backups — aviso de backup diario al entrar a la app | IMPLEMENTADO | Quien tenga `backups_crear` | Ya no es automático/silencioso (esa versión causaba backups fallidos y la app colgada, ver [DISASTER_RECOVERY.md](DISASTER_RECOVERY.md#backup-local-diario-por-usuario)) — ahora es un modal (`DailyBackupPrompt.tsx`) que el usuario confirma a mano, máximo un aviso resuelto por día por máquina, trackeado en `localStorage`, no en `backup_history` |
| Backups — verificación por restauración real en staging | **NO IMPLEMENTADO** | — | Hoy la verificación es estructural (tamaño, formato, conteo de filas contra el manifiesto embebido), no una restauración real en una BD de prueba — ver "Limitaciones conocidas" en [DISASTER_RECOVERY.md](DISASTER_RECOVERY.md) |
| Precios — captura masiva (Excel), consulta/edición por ficha, alta manual, agrupación por SKU principal | IMPLEMENTADO | `precios_ver`/`precios_modificar` (edición y alta manual); captura masiva es `isAdmin` | Tabla `precios` + `precios_historial` (solo se escribe, sin pantalla de consulta todavía). `PreciosModal` desde `ProductDetail` (botón "Precios" junto al SKU) incluye "Agregar nuevo producto" con validación de SKU duplicado (exacto, no `sku_principal`); `PreciosImportPanel` dentro de "Captura masiva" en Configuraciones |
| Remisiones — internas (crear, ver, editar, borrar, PDF sobre plantilla) | IMPLEMENTADO | `remisiones_acceso`/`remisiones_crear` (también gatea Editar)/`remisiones_cancelar` (ahora gatea "Borrar", no "Cancelar" — ver [PERMISSIONS.md](PERMISSIONS.md)) | `RemisionesSection`/`RemisionForm`/`RemisionDetalleModal`, folio generado dentro de `createRemisionConFolio`, PDF vía `buildRemisionPdf` (plantilla `Assets/remision-template.png`, rasterizada de `Assets/remision.pdf`). El buscador de renglones consulta `precios` (`searchPrecios`), no `products` — hay entrada manual (SKU+nombre a mano) para lo que no está en ninguna de las dos, con opción de "Guardar producto" (Interno/Externo) para reutilizarlo después. Editar conserva folio/fecha/creador (`updateRemisionConRenglones`); Borrar es borrado real (`deleteRemision`), no un flag. `pedido_bodegas` fijo en `"JALISCO"`, no capturable |
| Remisiones — externas | **PLANEADO** | `remisiones_acceso` | Selector "Tipo de remisión" existe y muestra "Externa" deshabilitada ("Próximamente"); sin lógica ni tablas propias |
| Lista de precios (con hoja "Externos") / Historial de remisiones (exportación Excel) | IMPLEMENTADO | `backups_ver` | Botones dentro de `BackupsPanel` (Configuraciones → Backups), no relacionados con el sistema de backups en sí — ver [PERMISSIONS.md](PERMISSIONS.md) |
| SKU Master (vista unificada de fichas/piezas/precios, asignar SKU a piezas sin uno, exportar todo a Excel) | IMPLEMENTADO | `sku_master` | `SkuMasterSection`, entrada propia en `Sidebar` debajo de "Remisiones". Agrupa `products`/`plastic_products`/`precios` por SKU principal (`computeSkuPrincipal`, mismo criterio que Precios) en modo solo lectura con enlaces a la ficha/pieza correspondiente; tab "Sin SKU" lista solo las piezas de `plastic_products` con `sku` vacío (única tabla donde el SKU no es obligatorio) con edición inline vía `updatePlasticProduct`. Botón "Exportar a Excel" (`getSkuMasterExportData`/`buildSkuMasterWorkbook`, mismo permiso `sku_master`, checado server-side) genera un `.xlsx` con 5 hojas (Productos/Desglose/Recetas/Precios/Remisiones) relacionables por SKU — ver [DATABASE.md](DATABASE.md). "Recetas" (costeo por juego, mismo formato que la hoja de trabajo "Para recetas" del negocio) es la única de las 5 con datos calculados en vez de solo pasar columnas de la BD — ver [WORKFLOWS.md](WORKFLOWS.md#importación-de-piezas-excel-dos-formatos). No agrega tablas ni columnas nuevas |

## Notas de precisión

- "Requisiciones" como *feature de negocio completa* (crear → surtir → cerrar) está a medias: la mitad de creación/notificación funciona end-to-end; la mitad de seguimiento/gestión no existe. No decir "implementado" sin esta aclaración.
- La importación por Excel **ya no es planeada** — está completamente implementada. Si se pidiera "agregar importación masiva", el trabajo real sería sobre `fichaImport.ts`/`FichaImportPanel.tsx` existentes, no un módulo nuevo.
